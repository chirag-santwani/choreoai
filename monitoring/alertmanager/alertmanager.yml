# AlertManager Configuration for ChoreoAI

global:
  resolve_timeout: 5m
  # Slack webhook URL (replace with your own)
  # slack_api_url: 'https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK'

# Route configuration
route:
  group_by: ['alertname', 'cluster', 'service', 'provider']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 12h
  receiver: 'default'

  routes:
    # Critical alerts go to multiple channels
    - match:
        severity: critical
      receiver: 'critical-alerts'
      continue: true

    # Warning alerts
    - match:
        severity: warning
      receiver: 'warning-alerts'
      continue: false

    # Cost-related alerts
    - match:
        component: cost
      receiver: 'cost-alerts'
      continue: false

# Receivers configuration
receivers:
  # Default receiver
  - name: 'default'
    # Uncomment and configure based on your notification preferences
    # webhook_configs:
    #   - url: 'http://localhost:5001/webhook'
    #     send_resolved: true

  # Critical alerts receiver (PagerDuty, Slack, Email, etc.)
  - name: 'critical-alerts'
    # Example: Slack configuration
    # slack_configs:
    #   - api_url: 'https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK'
    #     channel: '#choreoai-critical'
    #     title: '🚨 Critical Alert'
    #     text: '{{ range .Alerts }}{{ .Annotations.description }}\n{{ end }}'
    #     send_resolved: true

    # Example: PagerDuty configuration
    # pagerduty_configs:
    #   - service_key: 'YOUR_PAGERDUTY_KEY'
    #     description: '{{ .GroupLabels.alertname }}'

  # Warning alerts receiver
  - name: 'warning-alerts'
    # Example: Slack configuration
    # slack_configs:
    #   - api_url: 'https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK'
    #     channel: '#choreoai-warnings'
    #     title: '⚠️  Warning Alert'
    #     text: '{{ range .Alerts }}{{ .Annotations.description }}\n{{ end }}'
    #     send_resolved: true

  # Cost alerts receiver
  - name: 'cost-alerts'
    # Example: Email or Slack for cost monitoring
    # slack_configs:
    #   - api_url: 'https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK'
    #     channel: '#choreoai-cost'
    #     title: '💰 Cost Alert'
    #     text: '{{ range .Alerts }}{{ .Annotations.description }}\n{{ end }}'

# Inhibition rules
inhibit_rules:
  # Inhibit warning if critical is firing for same alert
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'cluster', 'service']

  # Don't alert about high latency if service is down
  - source_match:
      alertname: 'ServiceDown'
    target_match_re:
      alertname: '(HighLatency|VeryHighLatency)'
    equal: ['cluster', 'service']
